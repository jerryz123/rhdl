#lang rhdl
// Defines the canonical host-width-parameterized RHDL adder concisely.

circuit Adder(width):
  input(a, b): Bits(width)
  output sum: Bits(width)
  sum <== a + b

def design = elaborate(Adder(8))

export:
  Adder
  design
