#lang rhdl
// Defines the canonical host-width-parameterized frontend adder example.

module Adder(width: Int):
  input:
    a: Bits(width)
    b: Bits(width)
  output:
    sum: Bits(width)
  result = a + b
  sum := result

elaborate(Adder(8))
