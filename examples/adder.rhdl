#lang rhdl
// Defines the canonical host-width-parameterized embedded RHDL adder.

circuit Adder(width):
  def a = input("a", Bits(width))
  def b = input("b", Bits(width))
  def sum = output("sum", Bits(width))
  sum <== a + b

def design = elaborate(Adder(8))

export:
  Adder
  design
