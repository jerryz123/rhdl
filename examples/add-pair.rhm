#lang rhombus
// Defines a concise user combinator by importing only the combinational frontend module it needs.

import:
  "../rhdl/frontend/extensions/comb.rhm" open

export:
  add_pair

fun add_pair(left, right):
  left + right
