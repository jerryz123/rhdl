#lang rhombus
// Defines an example user-level combinator solely by importing the embedded kernel.

import:
  "../rhdl/frontend/kernel.rhm" open

export:
  add_pair

fun add_pair(left, right):
  hw_add(left, right, "layered_sum")
