#!/usr/bin/env bash
# Runs the fast parameter-annotation policy before creating a local commit.
set -euo pipefail

repo_dir="$(git rev-parse --show-toplevel)"
make -C "$repo_dir" check-parameter-annotations
