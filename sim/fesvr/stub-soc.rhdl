#lang rhdl
// Configures SimpleSoCTop with the processor stand-in for simulation smoke tests.

import:
  "simple-soc-top.rhdl".SimpleSoCTop
  "stub-core.rhdl".FesvrStubCore

def design = elaborate(SimpleSoCTop(FesvrStubCore(), 0x2000))

export:
  design
