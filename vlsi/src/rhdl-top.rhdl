#lang rhdl
// Defines the minimal combinational RHDL leaf used by the OpenFrame GDS smoke flow.

circuit RhdlTop():
  input in_bit: Bits(1)
  output out_bit: Bits(1)

  out_bit <== !in_bit

def design = elaborate(RhdlTop())

export:
  RhdlTop
  design
