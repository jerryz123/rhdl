#lang rfpl
// Invalid fixture supplying a zero physical length to a physical view.

import:
  "../support/annotation-circuits.rhm" as circuits

def bad = hard_macro(circuits.logic_logical.top, width: nm(0), height: um(10))
