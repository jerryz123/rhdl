#lang rfpl
// Invalid fixture assigning an RFPL placement coordinate to an RHDL circuit.

import:
  "../../../examples/hierarchy.rhdl".Adder

floorplan CoordinateOnCircuit():
  size(width: um(20), height: um(20))
  def adder = Adder(8)
  inst placed(adder, at: (um(0), um(0)))

def elaboration = elaborate(CoordinateOnCircuit())
