#lang rfpl
// Invalid fixture bypassing RFPL's coordinate-aware instance form through the kernel.

import:
  "../../../rhdl/frontend/kernel.rhm".inst as raw_inst

floorplan Child():
  size(width: um(5), height: um(5))

floorplan UntrackedCoordinate():
  size(width: um(10), height: um(10))
  def child = Child()
  raw_inst("placed", child)

def elaboration = elaborate(UntrackedCoordinate())
