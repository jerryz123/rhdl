#lang rfpl
// Invalid fixture supplying a negative physical length to a floorplan rectangle.

floorplan NegativeSize():
  size(width: um(-1), height: um(10))

def elaboration = elaborate(NegativeSize())
