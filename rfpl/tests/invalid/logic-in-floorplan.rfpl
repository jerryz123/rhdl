#lang rfpl
// Invalid fixture attempting to construct combinational logic inside a floorplan.

import:
  "../../../rhdl/frontend/layers/comb.rhm" open

floorplan BadLogic():
  size(width: um(10), height: um(10))
  input(a, b): Bits(8)
  output sum: Bits(8)
  sum <== a + b

def elaboration = elaborate(BadLogic())
