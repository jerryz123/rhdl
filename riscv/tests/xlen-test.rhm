#lang rhombus
// Checks the closed host-side XLEN configuration and width conversion.

import:
  "../isa/xlen.rhm" open

check:
  XLen.X32 is_a XLen ~is #true
  XLen.X64 is_a XLen ~is #true
  xlen_width(XLen.X32) ~is 32
  xlen_width(XLen.X64) ~is 64
  xlen_width(32) ~throws "XLen"
