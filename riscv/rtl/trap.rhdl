#lang rhodium
// Converts architectural synchronous exception causes into typed hardware values.

import:
  "../isa/trap.rhm" open

fun exception_cause_bits(cause :: ExceptionCause,
                         width :: PosInt) :~ Bits(width):
  bits(exception_code(cause), width)

export:
  exception_cause_bits
