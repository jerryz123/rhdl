#lang rhombus
// Defines the common host-side protocol for finite Ridx spaces and views.

export:
  IndexDomain

// Concrete spaces and subset views expose one uniform ordered-domain surface.
interface IndexDomain:
  method space()
  method points()
  method contains(point)
  method describe()
