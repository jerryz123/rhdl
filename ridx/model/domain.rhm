#lang rhombus
// Defines the common host-side protocol for finite Ridx spaces and views.

export:
  IndexDomain

// Domain operations remain in view.rhm because they depend on both concrete
// implementations; this protocol gives consumers an exact shared contract.
interface IndexDomain
