#lang rhombus
// Intentionally references an undeclared VC group from a directed link.

import:
  "../../../language/main.rhm" open

def bad = topology:
  node a
  node b
  directed broken:
    a -> b
    vcs [missing]
