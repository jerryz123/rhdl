#lang rhdl
// Provides parallel composition for generic interface-handle shapes.

import:
  rhombus/meta open

// Combines independent handles or terminated sinks into one array-shaped
// topology object of the same kind.
expr.macro 'parallel($path, ...)':
  'parallel_handles(Array($path, ...))'

export:
  parallel
