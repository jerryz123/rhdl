#lang rhdl
// Provides parallel composition for generic interface-handle shapes.

import:
  rhombus/meta open

// Combines independent interface handles into one array-shaped handle.
expr.macro 'parallel($handle, ...)':
  'parallel_handles(Array($handle, ...))'

export:
  parallel
