#lang rhombus
// Emits the width-changing datapath fixture as CIRCT MLIR for integration tests.

import:
  "../rhdl/main.rhm" open
  "support.rhm" open

def [design, _, _, _, _, _, _, _, _] = make_width_ops_design()
print(emit_circt(design))
