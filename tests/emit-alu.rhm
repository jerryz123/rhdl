#lang rhombus
// Emits the host-width-parameterized ALU fixture as CIRCT MLIR for integration tests.

import:
  "../rhdl/main.rhm" open
  "support.rhm" open

def [design, _, _, _, _, _, _, _] = make_alu_design()
print(emit_circt(design))
