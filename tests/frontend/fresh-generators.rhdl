#lang rhdl
// Confirms repeated calls elaborate fresh definitions instead of deduplicating by parameter.

module Adder(width: Int):
  input:
    a: Bits(width)
    b: Bits(width)
  output:
    sum: Bits(width)
  sum := a + b

module Top(width: Int):
  Adder(width)
  Adder(width)

elaborate(Top(8))
