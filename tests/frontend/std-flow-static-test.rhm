#lang rhombus
// Loads the use_static flow fixture so topology propagation is compile-tested.

import:
  "std-flow-static.rhdl" as fixture

check fixture.FlowStaticInformation is_a Function ~is #true
