#lang rhdl
// Must reject non-Int values passed to an Int generator parameter.

module BadParameter(width: Int):
  input:
    a: Bits(width)
  output:
    y: Bits(width)
  y := a

elaborate(BadParameter(#true))
