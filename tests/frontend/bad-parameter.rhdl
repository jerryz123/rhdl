#lang rhdl
// Must reject non-Int values supplied as circuit parameters.

circuit BadParameter(width):
  def a = input("a", Bits(width))
  def y = output("y", Bits(width))
  y <== a

def design = elaborate(BadParameter(#true))
