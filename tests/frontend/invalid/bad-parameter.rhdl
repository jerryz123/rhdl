#lang rhdl
// Invalid fixture supplying a non-Int value as a circuit parameter.

circuit BadParameter(width):
  input a: Bits(width)
  output y: Bits(width)
  y <== a

def design = elaborate(BadParameter(#true))
