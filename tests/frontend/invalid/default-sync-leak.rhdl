#lang rhdl
// Ensures the curated standard profile does not implicitly enable sync circuits.

sync_circuit UnexpectedSync():
  output value: Bits(1)
  value <== bits(0, ~width: 1)

def design = elaborate(UnexpectedSync())
