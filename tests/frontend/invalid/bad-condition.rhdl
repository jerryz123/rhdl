#lang rhdl
// Invalid fixture using hardware data as an ordinary Rhombus host condition.

circuit BadCondition(width):
  def a = input("a", Bits(width))
  if a | #void | #void

def design = elaborate(BadCondition(8))
