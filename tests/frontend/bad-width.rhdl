#lang rhdl
// Must report the original source line for a frontend addition width mismatch.

module BadWidth():
  input:
    a: Bits(4)
    b: Bits(8)
  output:
    sum: Bits(4)
  sum := a + b

elaborate(BadWidth())
