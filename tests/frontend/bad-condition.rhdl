#lang rhdl
// Must reject hardware data used as an ordinary Rhombus host condition.

circuit BadCondition(width):
  def a = input("a", Bits(width))
  if a | #void | #void

def design = elaborate(BadCondition(8))
