#lang rhdl
// Must reject circuit construction outside an elaborate expression.

circuit BadTopCall(width):
  def a = input("a", Bits(width))
  def y = output("y", Bits(width))
  y <== a

BadTopCall(8)
