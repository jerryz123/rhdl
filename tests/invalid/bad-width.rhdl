#lang rhdl
// Invalid fixture adding embedded hardware values with mismatched widths.

circuit BadWidth():
  def a = input("a", Bits(4))
  def b = input("b", Bits(8))
  def sum = output("sum", Bits(4))
  sum <== a + b

def design = elaborate(BadWidth())
