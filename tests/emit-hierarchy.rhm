#lang rhombus
// Emits the hierarchical fixture as CIRCT MLIR for backend integration tests.

import:
  "../rhdl/main.rhm" open
  "support.rhm" open

def [design, _, _, _, _, _] = make_hierarchy_design()
print(emit_circt(design))
