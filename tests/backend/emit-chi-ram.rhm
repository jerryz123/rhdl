#lang rhombus
// Emits CIRCT MLIR for the initial non-coherent CHI RAM fixture.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../chi/tests/ram-fixture.rhdl" as fixture

print(emit_circt(fixture.ram_design))
