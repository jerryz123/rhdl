#lang rhombus
// Emits the adder fixture as CIRCT MLIR for backend integration tests.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../examples/adder.rhdl" as fixture

print(emit_circt(fixture.design))
