#lang rhombus
// Emits CIRCT MLIR for Ricket's RV64 commit-owned CSR file fixture.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../rhdl/frontend/kernel.rhm" as embedded
  "../../riscv/isa/xlen.rhm" open
  "../../cores/ricket/csr.rhdl" as csr

def design = embedded.run_elaboration(fun (): csr.RicketCsrFile(XLen.X64))
print(emit_circt(design))
