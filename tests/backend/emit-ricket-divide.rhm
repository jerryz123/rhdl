#lang rhombus
// Emits CIRCT MLIR for Ricket's commit-issued integer-division fixture.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../rhdl/frontend/kernel.rhm" as embedded
  "../../riscv/isa/xlen.rhm" open
  "../../cores/ricket/core.rhdl" as core

def design = embedded.run_elaboration(fun (): core.RicketCore(XLen.X64))
print(emit_circt(design))
