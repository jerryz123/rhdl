#lang rhombus
// Emits CIRCT MLIR for the RV32 specialization of Ricket's core pipeline.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../rhdl/frontend/kernel.rhm" as embedded
  "../../riscv/isa/xlen.rhm" open
  "../../cores/ricket/core.rhdl" as pipeline

def design = embedded.run_elaboration(fun (): pipeline.RicketPipeline(XLen.X32, 32))
print(emit_circt(design))
