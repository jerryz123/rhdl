#lang rhombus
// Emits CIRCT MLIR for the initial non-coherent CHI transaction monitor fixture.

import:
  "../../rhodium/backend/circt.rhm" open
  "../../chi/tests/transaction-fixture.rhdl" as fixture

print(emit_circt(fixture.transaction_rni_design))
