#lang rhombus
// Emits CIRCT MLIR for record-typed ports crossing a module instance.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../rhdl/frontend/kernel.rhm" as embedded
  "../../examples/rhdl/bundle.rhdl" as bundle_fixture

def design = embedded.run_elaboration(fun (): bundle_fixture.BundleHierarchy(8))
print(emit_circt(design))
