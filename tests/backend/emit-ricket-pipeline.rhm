#lang rhombus
// Emits CIRCT MLIR for Ricket's scoreboarded pipeline fixture.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../rhdl/frontend/kernel.rhm" as embedded
  "../../riscv/isa/xlen.rhm" open
  "../../cores/ricket/core-pipeline.rhdl" as pipeline

def design = embedded.run_elaboration(fun (): pipeline.RicketPipeline(XLen.X64, 32))
print(emit_circt(design))
