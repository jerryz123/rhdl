#lang rhombus
// Emits CIRCT IR for the reusable load/store lane-generation fixture.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../rhdl/frontend/kernel.rhm" as embedded
  "../../cores/tests/load-store-fixture.rhdl" as fixture

def design = embedded.run_elaboration(fun (): fixture.LoadStoreFixture(64))
print(emit_circt(design))
