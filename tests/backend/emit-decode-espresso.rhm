#lang rhombus
// Emits the automatically Espresso-minimized typed decode example for simulation.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../examples/std/decode.rhdl" as fixture

print(emit_circt(fixture.design))
