#lang rhombus
// Emits CIRCT MLIR for the concrete combinational RV64I integer ALU.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../rhdl/frontend/kernel.rhm" as embedded
  "../../cores/alu.rhdl" as alu

def design = embedded.run_elaboration(fun (): alu.ALU())
print(emit_circt(design))
