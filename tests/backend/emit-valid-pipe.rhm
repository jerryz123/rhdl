#lang rhombus
// Emits CIRCT IR for the fixed-latency Valid pipeline example.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../examples/std/valid-pipe.rhdl" as fixture

print(emit_circt(fixture.design))
