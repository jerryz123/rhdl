#lang rhombus
// Emits CIRCT MLIR for RV5Stage's scoreboarded core fixture.

import:
  "../../rhdl/backend/circt.rhm" open
  "../../rhdl/frontend/kernel.rhm" as embedded
  "../../riscv/isa/xlen.rhm" open
  "../../cores/rv5stage/core.rhdl" as core

def design = embedded.run_elaboration(fun (): core.RV5StageCore(XLen.X64))
print(emit_circt(design))
