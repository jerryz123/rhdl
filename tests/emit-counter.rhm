#lang rhombus
// Emits the synchronous-reset counter fixture as CIRCT MLIR for backend integration tests.

import:
  "../rhdl/main.rhm" open
  "support.rhm" open

def [design, _, _, _, _] = make_counter_design()
print(emit_circt(design))
